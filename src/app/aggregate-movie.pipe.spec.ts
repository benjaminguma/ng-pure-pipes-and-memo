import { AggregateMoviePipe } from './aggregate-movie.pipe';

describe('AggregateMoviePipe', () => {
  it('create an instance', () => {
    const pipe = new AggregateMoviePipe();
    expect(pipe).toBeTruthy();
  });
});
